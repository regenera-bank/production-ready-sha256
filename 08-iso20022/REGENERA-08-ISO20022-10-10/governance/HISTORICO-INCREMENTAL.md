# Histórico incremental

- `ISO-101` — inventário físico e identificação da ausência de XSD e código executável;
- `ISO-107` — definição dos quatro perfis internos;
- `ISO-114` — parser seguro e limites de recurso;
- `ISO-123` — regras de `pacs.008` e `pacs.002`;
- `ISO-131` — regras de `camt.053` e `camt.054`;
- `ISO-139` — idempotência, digest e estado `UNKNOWN`;
- `ISO-147` — reconciliação e runbooks;
- `ISO-155` — controles, evidência, SBOM e release reproduzível;
- `ISO-161` — revisão final e bloqueios externos.

Os identificadores registram a sequência lógica do trabalho.
O histórico Git assinado deve ser criado no repositório institucional com a chave real do responsável.
