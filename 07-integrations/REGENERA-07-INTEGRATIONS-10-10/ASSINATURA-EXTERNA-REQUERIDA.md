# Assinatura externa requerida

A release não contém chave privada.
A assinatura `.asc` deve ser produzida fora do ZIP, com a chave real do responsável autorizado.

Arquivos esperados após assinatura:

- `REGENERA-07-INTEGRATIONS-10-10.zip.asc`;
- `REGENERA-07-INTEGRATIONS-10-10.zip.sha256.asc`.

Sem assinatura verificável, a release permanece bloqueada para promoção.
