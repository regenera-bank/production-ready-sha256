# Aplicação

1. extraia em diretório limpo;
2. execute `make all`;
3. revise `evidence/RELEASE-VERIFICATION.json`;
4. faça commit assinado no repositório canônico;
5. gere `.asc` com a chave real;
6. promova somente após aprovação independente.
