.PHONY: clean validate build test security evidence verify-release all

clean:
	npm run clean

validate:
	npm run validate

build:
	npm run build

test:
	npm test

security:
	npm run security

evidence:
	npm run evidence

verify-release:
	npm run verify-release

all:
	npm run all
