# Regenera — Vercel

Este diretório já é a raiz do projeto Vite. O vínculo `.vercel/project.json` e o `.env` recebidos foram preservados.

A fonte é a versão reorganizada do `customer-web`; `node_modules`, históricos e HTMLs concorrentes não entraram.

Antes de publicar, leia `docs/DEPLOY-VERCEL.md` e mova segredos para Environment Variables do projeto.
